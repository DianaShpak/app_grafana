---
- hosts:
    - app_type_grafana
  user: root
  become: true
  become_method: sudo
#  gather_facts: false
  roles:
    - docker_install

- hosts:
    - app_type_grafana
  user: root
  become: true
  become_method: sudo
  #  gather_facts: false
  roles:
    - deploy_grafana

- hosts:
    - app_type_http
  user: root
  become: true
  become_method: sudo
#  gather_facts: false
  roles:
    - deploy_exporter